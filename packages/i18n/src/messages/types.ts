export type Messages = {
  app: {
    name: string
    tagline: string
  }
  task: {
    add: string
    empty: string
    completed: string
    pending: string
  }
  list: {
    daily: string
    shared: string
    share: string
  }
}
