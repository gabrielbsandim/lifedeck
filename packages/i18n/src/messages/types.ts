export type Messages = {
  app: {
    name: string
    tagline: string
  }
  nav: {
    today: string
    lists: string
    analytics: string
  }
  task: {
    add: string
    empty: string
    completed: string
    pending: string
    allDone: string
    progress: string
  }
  list: {
    daily: string
    shared: string
    share: string
  }
}
